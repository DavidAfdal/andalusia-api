import { Request, Response, NextFunction } from 'express';

export const listEvent = (req: Request, res: Response, next: NextFunction) => {};

export const daftarEvent = (req: Request, res: Response, next: NextFunction) => {};

export const listPesertaEvent = (req: Request, res: Response, next: NextFunction) => {};
